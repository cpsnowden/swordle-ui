import { Container } from "@mui/material";
import { GamePanel } from "components/Wordle/GamePanel";

const Game = () => {
  return (
    <Container>
      <GamePanel/>
    </Container>
  )
};

export default Game;
